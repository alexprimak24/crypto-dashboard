// import React from 'react';

export default function Api() {
  return (
    <div>
      This is just for the future so ignore it for now but then I can make some
      real auth with firebase or appwrite
    </div>
  );
}
